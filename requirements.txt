# ── LAAP 依赖声明 ──────────────────────────────────────────
# 唯一事实源: pyproject.toml ([project].dependencies / optional-dependencies)
# 本文件仅为传统 pip 工作流提供入口, 不再维护第二份依赖清单。
#
# 安装:
#   pip install -e .            # 运行时依赖 (含 hermes-agent, 见 VERSIONS.yaml)
#   pip install -e ".[dev]"     # 附加开发依赖 (pytest, pytest-asyncio)
#
# 层级说明 (完整策略见 LICENSING.md):
#   CORE    = pyproject dependencies
#   OPTIONAL= pyproject optional-dependencies (psi-core / dev)
-e .
